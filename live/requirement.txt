pybit==5.5.5               # For Bybit Unified Trading API
requests>=2.28.0           # For HTTP requests to Hyperliquid and others
pprintpp>=0.4.0            # Pretty printing (optional but helpful for CLI)
python-dotenv>=1.0.0       # For loading API keys securely from .env if used
colorama>=0.4.6            # Optional: for CLI color formatting (visual enhancement)
